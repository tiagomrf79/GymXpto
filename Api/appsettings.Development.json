{
  "Logging": {
    "LogLevel": {
      "Default": "Debug", // debug (detailed info) + information (context) + warning + error + fatal
      "Microsoft.AspNetCore": "Trace" // trace (extremely detailed info) + debug + information + warning + error + fatal
    }
  },
  "ConnectionStrings": {
    "GymXptoConnectionString": "Server=(localdb)\\MSSQLLocalDB;Database=GymXptoTestingDb;Trusted_Connection=True;"
  },
  "AllowedHosts": "*" //any host is allowed access to the application, not recommended for production environments
}
